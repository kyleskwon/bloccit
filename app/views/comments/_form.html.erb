 <%= form_for [post, comment], remote: true do |f| %>
   <div class="form-group">
     <%= f.label :body, class: 'sr-only' %>
     <%= f.text_field :body, class: 'form-control', placeholder: "Enter a new tidbit" %>
   </div>
   <%= f.submit "Submit", class: 'btn btn-default' %>
 <% end %>
